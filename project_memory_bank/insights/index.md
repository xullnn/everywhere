# Insights Index

- `macos_tcc_codesigning.md`: Stable code signing is required to avoid repeated TCC permission churn during local macOS app development.
- `funasr_local_runtime.md`: Local FunASR websocket validation is more reliable when models are cached explicitly and stop control messages flush pending PCM to offline ASR.
